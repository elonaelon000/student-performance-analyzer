# Student Performance Analyzer

A Python data-analysis project that validates student CSV data, calculates academic metrics with pandas, studies the relationship between study hours and grades, and presents the results through both a command-line workflow and an interactive Streamlit dashboard.

**Live demo:** https://student-performance-analyzer-elonagit.streamlit.app/

## Technologies

- Python
- pandas
- Matplotlib
- Streamlit
- CSV
- unittest
- GitHub Actions
- Git and GitHub

## Project Structure

```text
student-performance-analyzer/
|-- app.py                    # command-line interface
|-- analysis.py               # validation and analysis logic
|-- dashboard.py              # dashboard data preparation
|-- streamlit_app.py          # Streamlit web interface
|-- visualizations.py         # Matplotlib file-based charts
|-- requirements.txt
|-- data/
|   `-- students.csv
|-- tests/
|   |-- test_analysis.py
|   |-- test_dashboard.py
|   `-- test_visualizations.py
|-- .github/
|   `-- workflows/
|       `-- tests.yml
|-- output/                   # generated by the CLI when it runs
|-- .gitignore
`-- README.md
```

## Setup

Clone the repository and enter the project folder:

```bash
git clone https://github.com/elonaelon000/student-performance-analyzer.git
cd student-performance-analyzer
```

Create and activate a virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

Install the dependencies:

```bash
python3 -m pip install -r requirements.txt
```

## Run the Streamlit Dashboard

```bash
streamlit run streamlit_app.py
```

The dashboard uses the included sample dataset by default. You can upload another CSV from the sidebar without changing the source code.

The uploaded file must contain:

```text
name,math,programming,statistics,study_hours
```

## Dashboard Features

The web interface displays:

- number of students analyzed
- overall class average
- pass rate
- top-performing student
- Pearson correlation between study hours and overall average
- a ranking table with a minimum-average filter
- an interactive subject-average bar chart
- an interactive study-hours scatter plot

Correlation is presented as an association in the available dataset and is not treated as proof of causation.

## Run the Command-Line Analyzer

The command-line workflow remains available:

```bash
python3 app.py
```

It prints the analysis report and creates two PNG files in `output/`:

```text
output/
|-- subject_averages.png
`-- study_hours_vs_average.png
```

## Data Validation

The same validation layer is used by the command-line application and the Streamlit upload workflow.

Validation rules include:

- required CSV columns must be present
- names cannot be empty
- grades must be numeric and between `0` and `100`
- study hours must be numeric and cannot be negative
- malformed or empty CSV files produce readable errors

## Run the Tests

```bash
python3 -m unittest discover -s tests -v
```

## Continuous Integration

GitHub Actions runs the test suite for pull requests and pushes to `main`. The workflow also compiles the Streamlit entrypoint to catch syntax errors.

## Deployment

The Streamlit dashboard is deployed at:

https://student-performance-analyzer-elonagit.streamlit.app/

## Why I Built This

I am developing this project incrementally to practice software engineering and data-science concepts. Version 1 established validation and testing with the Python standard library. Version 2 introduced pandas, visualization, statistical analysis, and a modular structure. Version 3 adds a browser interface, reusable dashboard logic, user-supplied data, and continuous integration.

## Author

**Elona Tarja**  
Informatics Engineering Student  

